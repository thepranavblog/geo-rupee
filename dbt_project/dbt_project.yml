name: "geopolitical_rupee_pipeline"
version: "1.0.0"
config-version: 2

profile: "geopolitical_rupee_pipeline"

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  geopolitical_rupee_pipeline:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: incremental
      +unique_key: event_id
